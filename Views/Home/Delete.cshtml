@model Destination
@{
    ViewData["Title"] = "Delete Destination";
}

<div class="d-flex">
    <h1 class="flex-grow-1">Delete Destination</h1>
    <div class="flex-shrink-1">
        <button class="btn btn-primary" form="form" type="submit">Save</a>
    </div>

</div>
<hr />

<p>Are you sure you want to delete this destination?</p>
<p><strong>Name:</strong> @Model.Name</p>
<p><strong>Destination E-Mail:</strong> @Model.Recipient</p>
<p><strong>NextCloud Path:</strong> @Model.Path</p>


<form id="form" method="post" asp-route-id="@Model.Id" asp-action="Delete" asp-controller="Home">
</form>